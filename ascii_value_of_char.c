#include<stdio.h>
int main()
{   float a;
    float b; 
    float result;
	printf("enter a number of a:");
	scanf("%f", &a);
	printf("enter a number of b:");
	scanf("%f", &b);
	result=a*b;
	printf("the result is %f.", result);
}
